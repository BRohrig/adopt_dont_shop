<%= form_with url: "/admin/pet_applications/#{@application.id}", method: :patch, local: true do |form| %>
  <%= form.hidden_field :approve, value: approve %>
  <%= form.hidden_field :pet_id, value: "#{pet_id}" %>
  <%= form.submit "#{text}" %>
<% end %>